#include <iostream>
#include "accumulator.h"

int main() {
    
    Accumulator accum;
    accum.run();
    // for (int i = 0; i < 10; ++i) {
    //     accum->attach(i);
    //     accum->eval();
    // }
    return 0;
}